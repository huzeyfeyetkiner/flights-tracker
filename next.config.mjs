/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"www.hertz.com",
			"static.independent.co.uk",
			"blog.travelcarma.com",
		],
	},
}

export default nextConfig
